# Maintenance

Maintenance is simple, just back up `~/.dsci` directory on server running dsci runner it will contain:

- dsci reports
- dsci sqlite database


To restore from back just copy the directory to `~/.dsci` location

